# 研究生辅助报考系统部署指南

## 部署前准备

- JDK 1.8+
- Maven 3.6+
- MySQL
- Python 3.7+（可选，用于预测服务）
- 可写的文件上传目录

## 1. 数据库初始化

项目默认库名为 `pgs`，初始化脚本在 `sql/PGS.sql`。

### 创建数据库

```bash
mysql -u root -p
CREATE DATABASE pgs CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
```

### 导入脚本

```bash
USE pgs;
SOURCE /你的项目绝对路径/sql/PGS.sql;
```

或直接导入：

```bash
mysql -u root -p pgs < /Users/yourname/PostgraduateReferenceSystem/sql/PGS.sql
```

## 2. 修改配置

### `admin/src/main/resources/application.yml`

重点改这几项：

```yaml
server:
  port: 8080

pgs:
  profile: /Users/yourname/pgs/uploadPath

ds:
  key: your-deepseek-api-key
  url: https://api.deepseek.com/chat/completions
```

说明：
- `server.port` 建议先用 `8080`，避免 `80` 端口权限和占用问题。
- `pgs.profile` 必须改成你本机真实存在且可写的目录。
- `ds.key` 仅在使用 `ai-chat` 的 DeepSeek 聊天功能时需要，请自行申请并填写。
- 如果不使用 AI 问答功能，可以先不配这一项。

### `admin/src/main/resources/application-druid.yml`

修改数据库账号密码：

```yaml
spring:
  datasource:
    druid:
      master:
        url: jdbc:mysql://localhost:3306/pgs?useUnicode=true&characterEncoding=utf8&zeroDateTimeBehavior=convertToNull&useSSL=true&serverTimezone=GMT%2B8
        username: root
        password: your_password
```

## 3. 打包项目

在项目根目录执行：

```bash
mvn clean package -DskipTests
```

### 产物说明

当前 `admin/pom.xml` 的 `finalName` 是 `admin`，所以通常生成：

```text
admin/target/admin.jar
```

注意：仓库里 `ry.sh` / `ry.bat` 以及 README 里部分地方写的是 `pgs-admin.jar`，和当前 POM 不完全一致。部署时请以实际打包结果为准。

## 4. 启动主服务

### 方式一：直接启动

```bash
java -jar admin/target/admin.jar
```

### 方式二：脚本启动

macOS / Linux：

```bash
chmod +x ry.sh
./ry.sh start
```

Windows：

```bat
ry.bat start
```

如果脚本里引用的 jar 名称和实际产物不一致，先改脚本或直接用 `java -jar` 启动。

### 方式三：IDEA 启动

运行主类：

```text
com.pgs.PGSApplication
```

位置：

```text
admin/src/main/java/com/pgs/PGSApplication.java
```

## 5. 可选：启动 Python 预测服务

`pyweb/pyweb.py` 提供深度学习预测接口，默认端口为 `5000`。

### 启动步骤

```bash
cd pyweb
pip install flask flask-cors torch numpy pandas scikit-learn
python pyweb.py
```

### 模型文件

确保下面文件存在：

```text
pyweb/final_transformer_model.pth
```

## 6. 访问地址

- 主站：`http://localhost:8080/`
- Swagger：`http://localhost:8080/swagger-ui/index.html`
- Python 预测服务：`http://localhost:5000/metadata`

如果你把主服务端口改成了 `80`，访问地址也要相应改成 `http://localhost/`。

## 7. 默认账号

- 用户名：`admin`
- 密码：`admin123`

建议部署后立即修改默认密码。

## 8. 常见问题

### 端口启动失败
- 检查 `server.port` 是否被占用
- macOS 上建议先用 `8080`

### 数据库连接失败
- 检查 MySQL 是否启动
- 检查数据库名、用户名、密码
- 检查 `application-druid.yml`

### 文件上传失败
- 检查 `pgs.profile` 是否存在
- 检查目录是否有写权限

### Python 服务无法访问
- 检查 `pyweb.py` 是否启动
- 检查 `5000` 端口是否被占用
- 检查 `final_transformer_model.pth` 是否存在

## 9. 安全建议

- 不要把真实数据库密码提交到公共仓库
- 不要把 DeepSeek API Key 公开
- 生产环境建议关闭不必要的调试配置
- 如果不需要 Swagger，可关闭 `swagger.enabled`

